# Agent Orchestrator Dependencies

# CLI Framework
click>=8.4.1

# Environment Management
python-dotenv>=1.2.2

# Data Validation
pydantic>=2.13.4
jsonschema>=4.26.0

# Database
sqlalchemy>=2.0.50

# JIRA Integration
jira>=3.10.5

# LangGraph — state machine orchestration with resumable checkpointing
langgraph>=1.2.4
langchain-core>=1.4.2
langgraph-checkpoint-sqlite>=3.1.0

# LiteLLM — model abstraction layer
# Do NOT use litellm[proxy] extras: they pin rich==13.9.4 which conflicts with
# textual>=8.2.7 (requires rich>=14.2.0). Instead, install only the packages
# needed to run `litellm --config ... --port ...` as a subprocess proxy server.
litellm>=1.88.1
litellm-proxy-extras==0.4.73
fastapi>=0.136.3
uvicorn>=0.49.0
uvloop>=0.21.0; sys_platform != "win32"
gunicorn>=26.0.0
backoff>=2.2.1
apscheduler>=3.11.2
fastapi-sso>=0.21.0
boto3>=1.43.1

# MCP Server
mcp>=1.27.2

# TUI Framework
textual>=8.2.7

# OpenTelemetry — Day-0 local tracing (see docs/configuration.md for env vars)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
# Optional: install opentelemetry-exporter-otlp-proto-grpc for OTLP export
# pip install opentelemetry-exporter-otlp-proto-grpc

# Goose Integration
# Note: Install Goose separately following official documentation
# Goose is typically installed via: pip install goose-toolkit
# or as a standalone CLI tool
