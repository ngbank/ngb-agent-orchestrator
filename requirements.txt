# Agent Orchestrator Dependencies

# CLI Framework
click>=8.4.1

# Environment Management
python-dotenv>=1.0.0

# Data Validation
pydantic>=2.0.0
jsonschema>=4.0.0

# Database
sqlalchemy>=2.0.50

# JIRA Integration
jira>=3.5.0

# LangGraph — state machine orchestration with resumable checkpointing
langgraph>=1.2.4
langchain-core>=1.4.0
langgraph-checkpoint-sqlite>=3.1.0

# LiteLLM — model abstraction layer
# [proxy] extras required for running the ephemeral proxy server in goose_session().
litellm[proxy]>=1.0.0

# MCP Server
mcp>=1.27.2

# TUI Framework
textual>=0.50.0

# Goose Integration
# Note: Install Goose separately following official documentation
# Goose is typically installed via: pip install goose-toolkit
# or as a standalone CLI tool
