# Agent Orchestrator Dependencies

# CLI Framework
click>=8.4.1

# Environment Management
python-dotenv>=1.2.2

# Data Validation
pydantic>=2.13.4
jsonschema>=4.26.0

# Database
sqlalchemy>=2.0.50

# JIRA Integration
jira>=3.10.5

# LangGraph — state machine orchestration with resumable checkpointing
langgraph>=1.2.4
langchain-core>=1.4.2
langgraph-checkpoint-sqlite>=3.1.0

# LiteLLM — model abstraction layer
# Do NOT use litellm[proxy] extras: they pin rich==13.9.4 which conflicts with
# textual>=8.2.7 (requires rich>=14.2.0). Instead, install only the packages
# needed to run `litellm --config ... --port ...` as a subprocess proxy server.
litellm>=1.88.1
fastapi>=0.124.0
uvicorn>=0.33.0
gunicorn>=26.0.0

# MCP Server
mcp>=1.27.2

# TUI Framework
textual>=8.2.7

# Goose Integration
# Note: Install Goose separately following official documentation
# Goose is typically installed via: pip install goose-toolkit
# or as a standalone CLI tool
