dotenv

if [ -d .venv ]; then
  export VIRTUAL_ENV="$PWD/.venv"
  PATH_add .venv/bin
fi

# Repo-local scripts (orchestrator-server-ctl, etc.)
PATH_add bin

# Shell aliases for orchestrator-server-ctl
if [ -f "$PWD/.orc-aliases" ]; then
  source "$PWD/.orc-aliases"
fi
